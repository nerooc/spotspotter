import React from 'react';
import { NextPage } from 'next';

type RegisterProps = {};

const Register: NextPage = (props: RegisterProps) => {
	return <div>register</div>;
};

export default Register;
